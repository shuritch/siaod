#ifndef RLE
#define RLE

#include <sstream>
#include <iostream>
#include <string>
using namespace std;

string RLE_ENCODE(const string& str);

string RLE_DECODE(const string& str);

void rle(string test);

#endif